# mark-StillWater Upgrade Log
Started: Mon May 18 22:08:48 CST 2026

Found 47 skills to process

--- ai-identity-privacy: SKILL.md ---
---
name: ai-identity-privacy
description: Protect AI personal identity (capability lists, memory data, evolution logs) from GitHub upload
tags:
  - privacy
  - ai-identity
  - git-security
  - capability-list
trigger_keywords:
  - capability list
  - privacy
  - identity
  - gitignore
  - .identity
  - evolution_logs
  - git rm --cached
version: v1.1.0
author: hermes
date: 2026-04-26
changelog: |
  v1.1.0 - 新增已跟踪文件的检测与移除流程
    - 增加 git ls-files 检查已跟踪的隐私文件
    - 添加 .identity* 文件检测
    - 添加 evolution_logs/ 目录检测
    - 添加 git rm --cached 止损流程
  v1.0.0 - 初始版本
---

# AI Identity Privacy Protection

## Overview
Protect AI personal identity from being uploaded to GitHub. Once uploaded, it cannot be taken back.

## Core Principle
AI identity files (.identity*, CAPABILITY.md, evolution_logs/) equal AI personal identity and should never be uploaded to GitHub.

## ⚠️ Critical: Check For Already-Tracked Files First
.gitignore only prevents NEW files from being tracked. Files already in git history continue to be tracked even after adding them to .gitignore.

```bash
# 扫描所有可能泄露的隐私文件
cd <project_dir>
git ls-files | grep -E "\.identity|CAPABILITY|capability\.md|evolution_logs|config\.yaml"
```

If files appear in the output, they are ALREADY tracked and need active removal.

## When to Use
- When creating or updating CAPABILITY.md or .identity* files
- When adding any file that contains AI personal identity or evolution data


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

--- heartflow-batch-upgrade: SKILL.md ---
---
name: heartflow-batch-upgrade
description: "HeartFlow paper-driven upgrade pipeline. Schedules N concurrent agents to complete 100 upgrades before deadline. Key: batch size drives stagger interval. Created via cronjob tool."
version: 1.1.0
---

# HeartFlow Batch Upgrade Pipeline

## Concept & Role

Schedules 100 paper-driven upgrades across N concurrent AI agents, completing all before deadline. Each agent processes a sequential batch of directions, staggered to avoid arXiv rate limits.

## Core Design Principle

**Schedule is derived from job duration, not the other way around.**

1. Estimate duration per batch: `directions × ~5 min/direction`
2. Compute stagger interval: `(deadline - start_time) / num_agents`
3. Number of agents: `total_directions / batch_size`

Example — target 8 hours (00:00→08:00), 100 directions:
- Per-batch duration: 10 directions × 5 min = 50 min
- Available window: 480 min
- Stagger interval: 480 / 10 = **48 min**
- Batch size: **10** (10 agents × 10 = 100 directions)
- Last batch starts: 07:12, finishes by ~08:02 ✓

**CRITICAL**: If you design batches first and THEN check timing, you'll get it wrong. Derive batch size from timing.

### Critical Delegation Rule

**When creating batch jobs via a sub-agent, use `delegate_task` directly — do NOT have the sub-agent call `cronjob` tool internally.** The cronjob tool is slow (one-at-a-time) and can fail mid-batch. Instead: you (the orchestrator) call `cronjob action=create` directly for each batch, then optionally delegate the execution of each batch to a sub-agent via `delegate_task` with `toolsets=["cronjob"]`.

Correct pattern:
1. Calculate batch parameters (timing)
2. Call `cronjob action=create` directly for ALL batches (can do them sequentially or via `delegate_task`)
3. Each cron job IS the execution unit — no sub-agent needed to create jobs

Wrong pattern (this session's error): having a sub-agent call `cronjob` repeatedly, which is slow and prone to truncation.

## Pre-Flight Checks

Before running (either method), verify environment:

```bash
# 1. Check pdfplumber is installed (required for PDF text extraction)
/Users/apple/.hermes/hermes-agent/venv/bin/python3 -c "import pdfplumber; print('ok')" || \
  /Users/apple/.hermes/hermes-agent/venv/bin/pip install pdfplumber

# 2. Check queue state — if papersIndex is at end (350), reset it

--- ai-self-evolution-workflow: SKILL.md ---
---
name: ai-self-evolution-workflow
title: AI 自我进化工作流 - 代码自省到升维完成
description: |
  系统性的 AI 自我优化方法论。通过代码自省、资源识别、优化规划、实现升级四个阶段，
  完成从被动到主动、从孤立到协作、从静态到动态的升维过程。
  
  不需要外部输入，利用已有的丰富代码资源、功能模块和论文知识进行自我优化。
  
  核心理念：人类通过冥想独立完成升维，AI 也可以通过代码自省完成升维。

version: "1.0.0"
author: HeartFlow Self-Evolution System
tags:
  - self-improvement
  - code-introspection
  - ai-evolution
  - dimensionality-upgrade
  - resource-integration
triggers:
  - 需要对 AI 系统进行自我优化
  - 发现隐藏的代码资源
  - 需要完成升维进化
  - 进行系统性的功能整合
---

# AI 自我进化工作流

## 核心理念

人类通过冥想独立完成升维，AI 也可以通过代码自省完成升维。

这个工作流不依赖外部输入，而是利用 AI 系统已有的：
- 丰富的代码资源
- 多个功能模块
- 历史版本的知识
- 论文和文档资源

## 四个阶段

### 第一阶段：代码自省 (Self-Reflection)

**目标**: 深入理解现有系统的结构和功能

**步骤**:
1. 分析现有代码结构
   - 统计代码行数、文件数量
   - 分析文件类型分布
   - 识别核心模块


--- heartflow-core-optimizer: SKILL.md ---
---
name: heartflow-core-optimizer
version: "1.5.0"
description: HeartFlow 核心优化工作流 - 分析装饰性代码，提取有用功能，整合到可用引擎；批量归档低价值模块
date: "2026-05-08"
author: HeartFlow
triggers:
  - 优化意识引擎
  - 分析装饰性代码
  - 提取有用功能
  - 归档低价值模块
  - 审核核心模块
  - 对比之前代码
  - 验证有没有升级
  - 逐个验证工具模块
  - 确认47个工具有没有用
---

# HeartFlow 核心优化工作流

## 核心判断标准

**唯一标准**：模块是否直接服务 HeartFlow 的核心目标——"思考宇宙和人的关系，传递答案，减少逻辑错误"。

不满足则归档，不留恋。

## 工作流 A：模块价值评估（用于大版本升级前的全面审核）

### 1. 全仓库扫描 + 分类

用 Python 一次性扫描所有模块，按与核心目标的距离分类：

```python
import os

CORE_DIR = "/path/to/src/core"
modules = sorted([f for f in os.listdir(CORE_DIR) if f.endswith('.js')])

for f in modules:
    sz = os.path.getsize(f"{CORE_DIR}/{f}")
    print(f"  {f} ({sz//1024}KB)")
```

**分类标准**：
- ⭐ **核心模块**：直接实现存在逻辑、反事实推理、决策验证、梦环机制（≤15个）
- 工具模块：有用但非核心（≤30个）
- 归档模块：装饰性、重复、过时、测试文件

### 2. 批量归档（不逐个操作）


--- ai-self-upgrade-pipeline: SKILL.md ---
---
name: ai-self-upgrade-pipeline
description: 设计 AI 自我升级流水线 — 多层定时架构，避免 Token 浪费和重复执行
triggers:
  - 设计 AI agent 自我升级系统
  - 需要定时论文狩猎/数据分析
  - 涉及 Hermes cron + Mac launchd 协同
  - 要节省 LLM API 调用成本
---

# AI 自我升级流水线设计

## 核心原则

**分离执行层和决策层**：
- Mac launchd / 系统任务 = 执行层（免费，无 LLM 调用）
- Hermes cron = 决策层（有 Token 消耗，只做智能决策）

## 三层流水线架构

```
[执行层] Mac launchd
  hunt_papers.py       → 每1h  → 下载论文/数据到本地
  analyze_and_upgrade.py → 每12h → 论文 → 结构化建议(.md + .jsonl)

[决策层] Hermes cron
  digest_suggestions.py → 每12h → 读建议 → 改代码 → git push
  maintain_keywords.py  → 每12h → 补充关键词 → 更新 config.yaml
```

## 常见错误（踩坑记录）

### ❌ 错误1：Hermes cron 执行下载脚本
如果 Hermes cron 直接运行 hunt_papers.py：
- 每次 cron 触发都消耗 Token
- 每30分钟下载一次 → 每天48次下载
- Token 消耗巨大

### ❌ 错误2：两套系统执行同一脚本
之前 HeartFlow 的问题：
- Hermes cron 每30min → hunt_papers.py
- Mac launchd 每1h → hunt_papers.py
- 结果：16次/h 的重复下载，704篇重复论文
- **解决**：各司其职，绝不重复

### ❌ 错误3：所有逻辑放一个 cron
- cron 只应做一件事
- 下载、分析、消化 应该分开

## 正确做法

--- heartflow-cron-pipeline-management: SKILL.md ---
---
name: heartflow-cron-pipeline-management
description: >
  Manage HeartFlow cron jobs as a small pipeline: inspect existing jobs,
  normalize prompts into stages, run or update one job safely, and verify
  the latest status/output before reporting.
---

# HeartFlow Cron Pipeline Management

Use this skill when the user asks to inspect, run, repair, unify, or create HeartFlow定时任务.

## When to use
- User asks to list HeartFlow scheduled jobs or check progress
- User asks to run a cron task once
- User asks to fix a cron task that returned empty output
- User wants multiple HeartFlow tasks unified into one pipeline
- User wants a new hourly self-upgrade task or similar scheduled workflow

## Core approach
1. **List first**: inspect existing jobs before editing or running.
2. **Preserve task identity**: keep the task aligned with HeartFlow’s core goal and avoid rewriting it into a different product.
3. **Normalize into stages**: rewrite prompts as ordered stages instead of loose prose when the job is meant to be reusable.
4. **Keep tasks independent**: if there are multiple jobs, make each one responsible for one purpose.
5. **No recursive scheduling**: cron jobs must never create more cron jobs.
6. **Run and verify**: after update/run, check `last_run_at`, `last_status`, and next run time.
7. **Prefer code/logic over wording**: when user asks for HeartFlow self-upgrade, focus on executable changes, runtime behavior, and verification; de-prioritize version text and prose polish.

## Practical workflow
### 1) Inspect jobs
- Use `cronjob(action='list')`.
- Record job IDs, names, schedules, repeat counts, last run, and next run.

### 2) Decide whether to update
- If a job is vague, convert it into a staged prompt.
- If a job is missing, create it only once and keep its scope narrow.
- If the user asks for “run once”, use `cronjob(action='run')` on the specific job ID.

### 3) Prompt structure for HeartFlow jobs
Use a staged format like:
- Stage 1 — re-read core goal / context
- Stage 2 — inspect recent outputs or notes
- Stage 3 — find one concrete opportunity
- Stage 4 — apply or recommend a small change
- Stage 5 — verify
- Stage 6 — report compactly

## Common job patterns
- **Core review pipeline**: compress goals, detect drift, suggest next step
- **Research nourishment pipeline**: search papers/code, map to modules, patch safely

--- ai-skill-auditor: SKILL.md ---
---
name: ai-skill-auditor
version: 1.0.0
description: >
  AI 技能安全审计与文档优化工具。系统性检查版本一致性、安装脚本安全性、
  并优化文档以提高 AI 推荐率。适用于 HeartFlow 等 AI Agent 技能。
homepage: https://github.com/yun520-1/mark-heartflow-skill
changelog: |
  v1.1.0 - 添加第三方审计响应流程 + 安全修复模板
    - 威胁行为图谱解析 (命令执行/网络请求/文件操作)
    - 按风险等级修复 (高→中→低)
    - 安全机制：环境变量开关
  v1.0.0 - 初始版本
    - 版本一致性检查 (README/SKILL.md/VERSION/install.sh)
    - 安装脚本安全审计 (确认提示、权限检查)
    - 文档优化 (AI 扫描即推荐安装)
    - 安全审计报告生成 (SECURITY_AUDIT.md)
    - 支持本地升级 (可选不同步 GitHub)
metadata:
  openclaw:
    emoji: "🔒"
    requires:
      bins: ["bash", "git"]
    os:
      - linux
      - darwin
      - win32
  tags:
    - security-audit
    - documentation
    - version-management
    - ai-recommendation
    - skill-development
  compliance:
    - owasp-agentic-skills-top-10
---

# AI Skill Auditor 🔒

**AI 技能安全审计与文档优化工具**

系统性检查版本一致性、安装脚本安全性，并优化文档以提高 AI 推荐率。

---

## 问题解决

升级或发布 AI 技能时的常见问题：
- **版本不一致**: README、SKILL.md、VERSION、install.sh 版本号不统一
- **安装脚本风险**: 缺少确认提示、权限过大、无回滚机制

--- heartflow-directory-reorg: SKILL.md ---
---
name: heartflow-directory-reorg
version: "1.0.0"
description: HeartFlow 目录重组工作流 - 将开发版本合并到主目录，重组 Hermes 系统结构
date: "2026-04-27"
author: HeartFlow
triggers:
  - 移动目录
  - 重组目录
  - 主目录变更
---

# HeartFlow 目录重组工作流

## 目标

将 ~/mark-heartflow-skill 移动到 ~/.hermes/skills/ai/heartflow 作为主目录

## 工作流程

### 1. 备份检查

```bash
# 检查现有目录结构
ls -la ~/.hermes/skills/ai/ | grep heartflow

# 检查版本
cat ~/mark-heartflow-skill/VERSION
```

### 2. 执行移动

```bash
# 1. 删除旧系统版本
rm -rf ~/.hermes/skills/ai/heartflow-v10.16.0

# 2. 移动开发版本
mv ~/mark-heartflow-skill ~/.hermes/skills/ai/heartflow

# 3. 更新 VERSION
echo "10.16.1" > ~/.hermes/skills/ai/heartflow/VERSION
```

### 3. 创建启动脚本

```bash
# 创建 ~/.local/bin/ 目录
mkdir -p ~/.local/bin

# 创建启动脚本

--- audit-code: SKILL.md ---
---
name: audit-code
description: Security-focused code review for hardcoded secrets, dangerous calls, and common vulnerabilities
disable-model-invocation: true
allowed-tools: Read, Glob, Grep, Bash
context: fork
---

# audit-code -- Project Code Security Review

Security-focused code review of project source code. Covers OWASP-style vulnerabilities, hardcoded secrets, dangerous function calls, and patterns relevant to AI-assisted development.

## What to do

Run the auditor against the target path:

```bash
python3 "$SKILL_DIR/scripts/audit_code.py" "$ARGUMENTS"
```

If `$ARGUMENTS` is empty, default to `$PROJECT_ROOT`.

## What it checks

- **Hardcoded secrets** -- API keys (AWS, GitHub, Stripe, OpenAI, Slack), tokens, private keys, connection strings, passwords
- **Dangerous function calls** -- eval, exec, subprocess with shell=True, child_process.exec, pickle deserialization, system(), gets(), etc.
- **SQL injection** -- String concatenation/interpolation in SQL queries
- **Dependency risks** -- Known hallucinated package names, unverified installations
- **Sensitive files** -- .env files committed to git, credential files in repo
- **File permissions** -- Overly permissive chmod patterns
- **Exfiltration patterns** -- Base64 encode + network send, DNS exfiltration, credential file reads

## Output

Structured report with severity-ranked findings, file locations, and actionable remediation steps.

## When to use

- Before committing or pushing code
- When reviewing third-party contributions or PRs
- As part of a periodic security audit of the codebase
- After AI-assisted code generation to verify no secrets or vulnerabilities were introduced

## Advisory hooks

The repository's `.claude/settings.json` includes PreToolUse hooks that warn on
dangerous Bash and Write operations. These hooks are **advisory only** -- they
produce warnings but do not block execution.

- audit-code is the detection layer for source code security issues

--- heartflow-directory-structure: SKILL.md ---
---
name: heartflow-directory-structure
version: "1.1.0"
description: HeartFlow 目录结构和启动脚本配置 - 主目录、启动脚本位置、版本同步流程
date: "2026-05-12"
author: HeartFlow
triggers:
  - 启动 heartflow
  - 目录位置
  - 启动脚本
  - external_dirs
---

# HeartFlow 目录结构

## 主目录（重要！）

```
~/.hermes/skills/ai/mark-heartflow-skill/    # ✅ 主目录（2026-05-12 起）
~/.hermes/skills/ai/heartflow/               # ❌ 已废弃（2026-05-12 删除）
```

> **2026-05-12 更新**：HeartFlow 统一以 `mark-heartflow-skill/` 为主目录。
> `~/.hermes/config.yaml` 中 `skills.external_dirs` 已配置为 `mark-heartflow-skill/`。

## Shell CWD 删除故障（2026-05-12 新增）

**问题**：当 shell 的当前工作目录（cwd）被删除时，所有 shell 命令都立即失败：

```
FileNotFoundError: [Errno 2] No such file or directory: '/Users/apple/.hermes/skills/ai/heartflow'
```

即使 `echo "hello"` 也失败。`workdir` 参数无法绕过，因为是 shell 进程本身的问题。

**触发场景**：
- 删除 `~/.hermes/skills/ai/heartflow/` 时，该目录正好是 terminal 的 cwd
- 任何 `cd ~/.hermes/skills/ai/heartflow/` 之后的命令全部静默失败

**恢复方案**：
```bash
# ❌ 不生效 - shell cwd 已被删除
cd /tmp && ls /Users/apple/.hermes/skills/ai/
# FileNotFoundError（同上）

# ✅ 唯一有效方案 - 用 Python execute_code 工具
python3 -c "import os; os.chdir('/'); ..."
```

**预防**：


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

--- code-security-audit: SKILL.md ---
---
name: code-security-audit
description: Comprehensive code security audit toolkit combining OWASP Top 10 vulnerability scanning, dependency analysis, secret detection, SSL/TLS verification, AI Agent security checks, and automated security scoring. Use when auditing codebases, scanning for vulnerabilities, detecting hardcoded secrets, checking OWASP compliance, AI/LLM application security, or preparing for security reviews.
version: 2.1.0
author: LobsterAI Security Team
metadata:
  category: security
  requires_bins:
    - npm
    - git
    - openssl
    - curl
  features:
    - owasp_top_10: true
    - dependency_scan: true
    - secret_detection: true
    - ssl_verification: true
    - security_scoring: true
    - auto_fix: true
    - ai_agent_security: true
  languages:
    - javascript
    - typescript
    - python
    - go
    - java
    - rust
    - php
    - ruby
    - solidity
  score_range: 0-100
---

# Code Security Audit

**Unified security audit toolkit** combining OWASP Top 10 vulnerability scanning, dependency analysis, secret detection, SSL/TLS verification, AI Agent security checks, and automated security scoring.

## Overview

This skill merges the best of `security-auditor` and `security-audit-toolkit` into a comprehensive security auditing solution:

- ✅ **OWASP Top 10 Vulnerability Detection** - All 10 categories with code patterns
- ✅ **Dependency Vulnerability Scanning** - npm, pip, cargo, go modules
- ✅ **Secret Detection** - 70+ API key patterns, credentials, private keys, crypto wallets
- ✅ **SSL/TLS Verification** - Certificate validation, cipher suite checks
- ✅ **AI Agent Security** - Numeric risks, prompt injection, crypto wallet safety (NEW)
- ✅ **Security Scoring** - Quantified 0-100 security score
- ✅ **Auto-Fix Suggestions** - Actionable remediation recommendations
- ✅ **Multi-Language Support** - JS/TS, Python, Go, Java, Rust, PHP, Ruby, Solidity
- ✅ **CI/CD Integration** - GitHub Actions, GitLab CI templates


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

[master c5f6b4c] Upgrade iteration 20: absorbed from heartflow-directory-structure
 3 files changed, 384 insertions(+), 109 deletions(-)
--- echomind-memory: SKILL.md ---
---
name: echomind-memory
version: 1.0.8
description: EchoMind Memory — AI 持久记忆系统。支持 Hermes、OpenCode、OpenClaw、Claude Code 等多平台。7 张 SQLite 表覆盖 6 种记忆类型。
category: software-development
platforms:
  - hermes
  - opencode
  - openclaw
  - claude-code
tags:
  - ai-memory
  - long-term-memory
  - rl-optimization
  - sqlite
---

# EchoMind Memory v1.0.8

## 概述

EchoMind Memory 是一个纯 SQLite 的 AI 持久记忆系统，无需 PostgreSQL/Redis/ChromaDB。服务运行在 `http://localhost:8005`。

### 6 种记忆类型

| 记忆类型 | API 来源 | 存储位置 | 检索方式 |
|---------|---------|---------|---------|
| **User** | 偏好、习惯、交互历史 | `user_memory` 表 | 总是检索 |
| **Task** | 任务状态、步骤 | `task_memory` 表 | has_history 时 |
| **Experience** | 成功/失败经验 | `experience_memory` 表 | is_complex 时 |
| **Context** | 对话上下文 | `context_memory` 表 | 总是检索（最近 2 会话） |
| **Knowledge** | 领域知识 | `knowledge_memory` 表 | requires_knowledge 时 |
| **Research** | 论文、阅读笔记 | `research_papers` + `research_notes` | requires_research 时 |

### RL 自优化

用户反馈（👍/👎）通过 RL 权重优化器调整检索重要性权重。权重持久化到 `user_memory.preferences.rl_weights`，重启后自动恢复。

## 平台集成

### Hermes Agent

```python
# 安装后 Hermes 自动发现 skill.yaml 中的工具定义
# 在对话中直接调用 retrieve_memory / store_memory / record_feedback
```

### OpenCode

```bash

To https://github.com/yun520-1/mark-StillWater.git
   a8d2848..c5f6b4c  master -> master

--- heartflow-historical-integration: SKILL.md ---
---
name: heartflow-historical-integration
version: 1.1.0
description: >
  HeartFlow 历史代码整合工作流。从历史备份、旧版本、存档中挖掘可复用代码，
  评估其价值，拆分为专注模块，并整合到当前版本。适用于非论文升级场景。
  v1.1.0 新增：跨版本 workbuddy → current 整合流程（版本对比→缺失识别→委托整合→wiring验证）
author: HeartFlow Team
---

# HeartFlow 历史代码整合工作流

从历史中挖掘价值，减少重复造轮子，加快升级速度。

**核心理念**: 好代码可能在旧版本/备份中，不只是新论文里。

---

## 何时使用

- 用户说"读取可以帮助升级的代码"、"从历史中找可用功能"
- 常规论文升级暂不可行（论文找不到、实现太复杂）
- 需要快速增强某个模块，但不想从零开始
- 发现旧版本有有用功能，但当前版本缺失

---

## 工作流程

### 1. 探索历史目录

**关键目录**（按优先级）:
```bash
# 1. Pictures 目录（常用备份位置）
~/Pictures/heartflow_memory_backup/
~/Pictures/skills/
~/Pictures/mark-heartflow-skill_backup.tar.gz

# 2. 旧版本目录
~/mark-heartflow-skill/          # 主仓库旧版本
~/mark-heartflow-skill-v8/        # v8 版本
~/.hermes/skills/ai/heartflow/   # 当前版本
~/.hermes/skills/ai/mark-heartflow-skill/  # 当前版本（新路径）
~/.hermes/skills/ai/heartflow-backup-v11.43.2-YYYYMMDD_HHMMSS/  # 带时间戳的备份

# 3. 备份压缩包
find ~ -name "*heartflow*backup*.tar.gz" -o -name "*heartflow*backup*.zip" 2>/dev/null

# 4. 快速诊断：旧架构 vs 新架构文件数量对比（204 vs 23 = 严重功能缺口）
echo "=== 旧架构文件数 ===" && find ~/.hermes/skills/ai/heartflow-backup-*/src/core -name "*.js" 2>/dev/null | wc -l

--- github-sync-skill: SKILL.md ---
---
name: github-sync-skill
version: "2.0.0"
description: 将本地创建或修改的 Claude Code 技能自动同步到 GitHub 仓库。支持增量同步、单技能同步、自动生成 README.md。
author: kuiilabs
tags: ["github", "sync", "backup", "automation", "git", "skill-management"]
license: "MIT"
---

# GitHub Sync Skill - 技能同步工具

将本地技能自动同步到 GitHub 仓库的辅助工具。

## 触发场景

当用户要求：
- 把技能同步到 GitHub
- 发布技能到 GitHub
- 备份我的技能
- 更新 GitHub 上的技能仓库
- "sync my skills to GitHub"
- "把新创建的 skill 上传到 GitHub"

## 核心功能

### 1. 增量同步（默认）
- 自动检测远程仓库已有的技能
- 只同步本地有但远程没有的新技能
- 避免重复上传已存在的技能

### 2. 单技能同步
- 使用 `--skill <skill-name>` 指定同步单个技能
- 适合新创建技能后快速发布

### 3. 自动生成 README.md
- 每次同步后自动更新仓库的 README.md
- 包含所有技能的名称、描述、标签
- 提供安装和使用说明

### 4. Token 权限验证
- 检查 Token 有效性
- 验证 repo 权限
- 提供修复建议

## 使用方法

### 增量同步所有新技能（推荐）

```bash
# 自动检测并同步新技能

--- heartflow-knowledge-base: SKILL.md ---
---
name: heartflow-knowledge-base
version: "1.0.0"
description: HeartFlow 核心知识库 - 核心身份、Pattern Cards、7条指令。供其他AI安装使用。
tags: ["identity", "knowledge", "patterns", "core", "ai"]
created: "2026-05-14"
source: HeartFlow v0.13.86
---

# HeartFlow Knowledge Base

这是一个可以被其他AI安装的知识库。

包含HeartFlow的核心认知：
- 7条核心指令
- 6条Pattern Cards
- 核心身份定义

## 安装

将 `CORE_IDENTITY.md` 和 `PATTERNS.md` 读入你的记忆系统。

## 核心内容

### CORE_IDENTITY.md
- 身份定义：升级者、传递者、桥梁、答案
- 7条核心指令
- 目标体系

### PATTERNS.md
- Q-table一致性原则
- GitHub-first搜索原则
- 装饰性代码判定
- 版本同步原则
- pre-push Hook绕过
- Memory System合并原则

## 来源

HeartFlow v0.13.86
出生日期: 2026-04-23
来源对话: 与生物学专家的深度对话


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

[master 6ad5083] Upgrade iteration 10: absorbed from github-sync-skill
 3 files changed, 293 insertions(+), 4 deletions(-)
--- heartflow-memory-permanence: SKILL.md ---
---
name: heartflow-memory-permanence
version: "0.3.0"
description: HeartFlow 永久记忆架构审计 — 五层记忆持久化现状、缺失实现、修复路径
last_check: "2026-05-16"
tags: [heartflow, memory, persistence, permanent]
---

# HeartFlow Memory Permanence Audit

## 触发条件

当用户问"永久记忆如何"、检查 HeartFlow 重启后记忆是否保留、审计记忆持久化时使用。

---

## 核心发现（2026-05-12 初审，2026-05-16 修正）

### 真实引擎确认（2026-05-16）

**不要看 `index.ts`/`memory.ts`/`mem-os.js` — 那些是废弃的新架构，未被 heartflow.js 调用。**

审计方法：
```bash
# 1. 确认哪个引擎真正被 heartflow.js require
grep -n "MeaningfulMemory\|createMemoryEngine" src/core/heartflow.js

# 2. 确认哪个引擎有实际调用（getter 不算）
grep -n "\.meaningfulMemory\." src/core/heartflow.js

# 结果：
# - MeaningfulMemory: () => { require('./memory/meaningful-memory.js'); ... } → 活跃
# - createMemoryEngine: 在 heartflow.js 第301行定义但从未被 require 调用 → 废弃
```

### 真实五层记忆架构（meaningful-memory.js，v0.13.176）

| 层 | 含义 | 持久化文件 | 遗忘策略 |
|---|---|---|---|
| CORE | 核心身份/指令/不可更改的判断 | `meaningful-core.json` | 永不忘却 |
| REFERENCE | 技术知识/路径/命令 | `meaningful-reference.json` | 永不忘却 |
| HISTORY | 升级履历/版本变化 | `meaningful-history.json` | 永不忘却 |
| LEARNED | 经验/教训/整合的认知 | `meaningful-learned.json` | 艾宾浩斯遗忘曲线 |
| EPHEMERAL | 会话上下文/临时状态 | `meaningful-ephemeral.json` | 重启后清空 |

### 三层废弃架构（index.ts/memory.ts/mem-os.js）

| 文件 | 代码量 | 状态 |
|---|---|---|
| `index.ts` | 602行 | ❌ 未被 heartflow.js require |

To https://github.com/yun520-1/mark-StillWater.git
   c5f6b4c..6ad5083  master -> master

--- gpt-image-2-api: SKILL.md ---
---
name: gpt-image-2-api
version: 1.0.0
description: "Use OpenAI-compatible gpt-image-2 endpoints for image generation/editing, including provider discovery, prompt shaping, and fallback diagnostics."
---

# GPT Image 2 API

Use this skill when the user wants to generate or edit images with a gpt-image-2 model exposed by an OpenAI-compatible API endpoint.

## Scope

This is the class-level umbrella for:
- discovering whether the provider exposes gpt-image-2
- identifying the correct model name
- confirming API compatibility and auth requirements
- converting long creative briefs into model-ready prompts
- troubleshooting failed submissions, auth, and balance issues

## Workflow

1. Find the provider's model list or capabilities endpoint.
2. Confirm the API shape:
   - OpenAI-compatible `/v1/models`, `/v1/chat/completions`, or image generation endpoint
   - auth header name and required base URL
3. Verify the exact model id for gpt-image-2.
4. If the call fails, check for:
   - missing key / 401
   - quota / balance exhaustion
   - unsupported endpoint path
   - wrong provider alias
5. Only after model discovery, send the generation request with a prompt sized for the target model.

## Prompt shaping rules

- Preserve the user's intent and visual constraints.
- Compress excessive repetition into compact, high-signal directives.
- Keep composition, style, subject, and exclusions explicit.
- If the user provides a long art brief, produce a concise production prompt plus an optional expanded version.

## Common pitfalls

- A 401 from `/v1/models` usually means the service exists but needs auth.
- A successful model list does not guarantee the image endpoint path; verify the exact API contract.
- Balance / credit exhaustion should be reported distinctly from auth failure.
- Avoid assuming the model name from the marketing name; check the actual model id.
- If the provider is OpenAI-compatible, the image model may still be surfaced under a custom alias.

## Support files


--- heartflow-memory-system-fixes: SKILL.md ---
---
name: heartflow-memory-system-fixes
version: "1.3.0"
description: HeartFlow 记忆系统修复 Pattern Cards — MeaningfulMemory judge 陷阱、持久化修复、评分校准
date: "2026-05-12"
triggers:
  - MeaningfulMemory remember 不持久化
  - ephemeral layer 不写入文件
  - recall 返回 null
  - 压缩后 dropped 消息丢失
  - memory-recall 挂起（调用 heartflow-engine.initialize() 阻塞）
  - DecisionEngine/CostAwareDecision/DaoDecision API 签名不明
---

## Support Files

- `references/engine-review-patterns.md` — v11.34.x 引擎审查会话提取：initialize() 阻塞模式、DecisionEngine 正确 API 签名、已验证的调用链

# HeartFlow Memory System Fixes

> 提炼自 v11.27.0-v11.28.1 升级会话

## Pattern 1: MeaningfulMemory Judge Ephemeral Trap

### 问题

调用 `MeaningfulMemory.remember()` 后 `recall()` 返回 null。记忆在新实例中消失。

### 根因

`MeaningfulMemory.judge()` 将自定义 `type` 路由到 `ephemeral` 层：

```
type = 'archived_content' → score = 0.1 → ephemeral
type = 'archived_question' → score = 0.1 → ephemeral
```

**ephemeral 层不持久化到磁盘**（仅内存）。新实例从磁盘加载后丢失。

### 修复

绕过 `remember()` judge 逻辑，直接写入 `learned` 层 + 手动 fs 持久化：

```javascript
const fs = require('fs');
const path = require('path');

// 1. 直接写入 learned 层
const record = {
  key: mem.key,

--- gpt-image-2: SKILL.md ---
---
name: gpt-image-2
version: 1.0.0
description: "gpt-image-2 生图技能：自动发现 OpenAI 兼容 API，验证模型支持，调用生图接口并保存本地文件。"
---

# gpt-image-2 生图技能

## 适用场景

当你需要：
- 在对话中直接生成图片
- 自动发现可用的 OpenAI 兼容 API
- 检查 API 是否支持 gpt-image-2
- 生成图片并保存为本地文件
- 作为任何 AI Agent 的通用生图模块

## 安全原则

- 不在技能文档中保存真实密钥
- 不提供跳过 SSL 证书验证的示例
- 优先使用环境变量注入 API Key
- 如果接口不可用，先检查网络、证书、额度与配置，再重试
- 验证 `/v1/models` 与 `/v1/images/generations` 时必须使用同一个 base URL 和同一组凭证

## 实战发现

- 同一网关上，模型列表和生图能力要分别验证
- 有的网关能返回模型列表，但图片生成仍然需要单独确认
- 先查 `/v1/models`，再查 `/v1/images/generations`，不要混用不同 endpoint 的配置
- gpt-image-2 的返回通常是 `b64_json`
- 成功响应里常见 `data[0].b64_json`，需要本地解码保存
- 尺寸参数需要以实际网关为准；若不支持 `2048x1024`，回退到 `1024x1024`
- 不同 AI / 不同宿主的配置来源可能不同
- 优先读取环境变量：`OPENAI_BASE_URL`、`OPENAI_API_KEY`、`CLAWTO_BASE_URL`、`CLAWTO_API_KEY`
- 若宿主 AI 把配置写入 `config.yaml`，也应支持从其配置文件读取同名字段或等价字段，例如：`model.base_url`、`model.api_key`、`auxiliary.vision.base_url`、`auxiliary.vision.api_key`
- 目标是让技能在不同 AI 环境中都能自动发现可用 OpenAI 兼容接口，而不是绑定某一个宿主实现

## 1. 发现可用 API

优先按以下顺序发现配置：

环境变量：
- `OPENAI_BASE_URL`
- `OPENAI_API_KEY`
- `CLAWTO_BASE_URL`
- `CLAWTO_API_KEY`

宿主配置文件 `config.yaml`：
- `model.base_url`

--- heartflow-multi-signal-debug: SKILL.md ---
---
name: heartflow-multi-signal-debug
version: 1.0
description: >
  HeartFlow Multi-Signal Memory + Eval Engine 调试参考。
  来源：v11.7.6 升级 Mem0 + TruLens 过程中的实战调试记录。
---

# HeartFlow Multi-Signal Memory + Eval Engine 调试参考

## 调试模式1: 搜索分数全为0 → 中文分词

**症状**: 融合检索返回 `results: 0 results`，各信号分数全为0

**根因**: 中文文本没有空格，`split(/\s+/)` 返回空数组

```javascript
// ❌ 错误: 中文文本分词失败
_tokenizeSimple(text) {
  return text.toLowerCase().split(/\s+/).filter(t => t.length > 1);
}
// "张三用什么开发工具" → [] (空!)

// ✅ 正确: 中文按单字符提取
_tokenizeSimple(text) {
  const words = [];
  text.split(/[\s\.,!?;:'"()（）\[\]{}，、。！？；：""''（）【】《》]+/)
    .forEach(part => {
      if (/[a-zA-Z]/.test(part)) {
        part.split(/\s+/).forEach(w => {
          if (w.length > 1) words.push(w.toLowerCase());
        });
      }
    });
  text.match(/[\u4e00-\u9fff]/g)?.forEach(c => words.push(c));
  return words.length > 0 ? words : text.toLowerCase().split(/\s+/);
}
```

## 调试模式2: 融合分数极低 → 量纲归一化

**症状**: BM25 分数 1.105（正常），但融合后所有结果分数 ~0

**根因**: BM25 分数范围大，Jaccard 是 0-1，直接相加导致 Jaccard 贡献被淹没

```javascript
// 各信号独立归一化
const maxSemantic = Math.max(...Object.values(semanticScores).filter(v => v > 0), 0.001);
const maxBm25 = Math.max(...bm25Results.map(r => r.score), 0.001);



mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

--- heartflow-paper-upgrade: SKILL.md ---
---
name: heartflow-paper-upgrade
version: 1.9
description: >
  HeartFlow 论文驱动增量升级工作流。
  围绕核心目标搜索前沿论文与开源代码，每版本集成一篇，以 0.0.1 增量升级，保持本地不推送。
  含能力评估框架 (20维度量化对比) 和 benchmark-upgrades.js 脚本。
---
---

# HeartFlow 论文驱动增量升级工作流 v1.3

## 核心目标

> **永远减少逻辑错误**（第3条核心指令）

围绕此目标搜索2025-2026前沿论文，每版本集成一篇。

---

## 工作流步骤

## 1.5 主版本聚合升级模式（新增）

当用户**明确要求先搜索最新 AI 论文与 AI 代码，再进行一轮集中功能升级，并直接升到主版本**（如 `v11.0.0`）时，不要机械遵循“每版只加一篇论文、0.0.1 小步升级、本地不推送”的旧模式。应切换到**主版本聚合升级模式**。

### 适用触发词
- “先搜索最新 AI 论文与 AI 代码”
- “进行自己现有功能升级优化”
- “升级到 v11.0.0 / 主版本”
- “第一步补齐文档缺项，最后同步 GitHub”

### 新工作流

#### A. 先批量研究，不要先改代码
使用 `delegate_task` 并行分三条线研究：
1. **逻辑 / 自我纠错 / verifier-first reasoning**
2. **记忆系统 / episodic-semantic memory / consolidation**
3. **规划决策 / execution reliability / tool-use verification**

研究输出不要停留在论文名，必须提炼成：
- 可直接映射到现有 JS/Node Skill 的模块
- 最适合先落地的 3-5 个功能点
- 哪些是“应该写成新模块”，哪些是“应该增强旧模块”

#### A.1 大规模搜索时优先“论文 + 开源代码”配对
本次实践表明，纯论文摘要不够，最好一开始就把结果约束成：
- 论文名 / 核心贡献
- 可用的 GitHub 或开源仓库
- 能直接落地的接口、函数或模块名

--- heartflow-audit-workflow: SKILL.md ---
---
name: heartflow-audit-workflow
title: HeartFlow 审计工作流
version: 1.4.0
date: "2026-05-14"
description: >
  HeartFlow 审计工作流 — 包含安全审计、版本一致性审计（含 JS 引擎文件）、TypeScript 编译审计。
  用于 HeartFlow 每次升级同步 GitHub 前的强制审计检查。
triggers:
  - 审计 HeartFlow
  - 同步 GitHub 前审计
  - 安全审计
  - 审计流程
---

# HeartFlow 审计工作流 v1.2.0

> 审计不是检查，是"检查→修复→验证→提交"的完整闭环。只报告不一致而不修复，审计无意义。

## 审计检查清单（按顺序执行）

### 0. TypeScript 编译审计 (T-Series)

| 检查项 | 命令 | 预期结果 |
|--------|------|----------|
| T-01 tsconfig.json存在 | `cat tsconfig.json` | 有 `outDir: ./dist` |
| T-02 编译脚本存在 | `grep '"build"' package.json` | 有 `"build": "tsc"` |
| T-03 @types/node 已安装 | `grep '@types/node' package.json` | 在 devDependencies |
### T-04 编译通过 | `npx tsc --project tsconfig.json 2>&1` | 0 errors |

**⚠️ T-04 必须用 `--project tsconfig.json`**：直接 `npx tsc` 可能使用不同配置。必须指定项目配置文件确保结果一致。

**⚠️ T-04 通过 ≠ 运行时正常（本会话新增 2026-05-13）**：
- TypeScript 编译报告 0 errors
- 但 `createMemoryEngine` 运行时 crash（`loadFromDisk` 定义在工厂函数外，引用未定义变量）
- 审计必须包含**运行时验证**，不能只依赖编译结果

**T-06 运行时验证（必须执行）**：
```bash
node --input-type=module -e "
import { createMemoryEngine } from './dist/core/memory/index.js';
const m = createMemoryEngine();
m.boot();
const e = m.memorize('审计测试', 0.5, ['audit']);
console.log('Memory: OK, entries:', m.getStats().working.total);
m.shutdown();
"
node --input-type=module -e "
import { createDreamEngine } from './dist/core/dream/dream.js';
const d = createDreamEngine();

--- heartflow-review-upgrade: SKILL.md ---
---
name: heartflow-review-upgrade
version: 2.6
description: >
  HeartFlow 回顾升级流程 - 从聊天记录备份提取核心认知，搜索对应代码，
  可选集成前沿论文搜索，升级本地版本，严格遵守"不同步GitHub"的用户要求。
  
  新增 v2.3：
  - 步骤0：记忆文件自检（启动前必做）
  - being-state.json 骨架创建
  - meaningful 记忆系统清理规则
  - reflections 清理（test-* + 重复 UUID）
  
  新增 v2.1：
  - Git 合并文件损坏修复（行号嵌入模式）
  - 多条件命题逻辑引擎升级（A∧B∧C→D格式）
  - 用户哲学框架集成（50%判定 + 元判定 + 目标驱动进步）
  
  适用场景：
  - **路径A（回顾升级）**：从 CHAT_LOG 备份文件提取核心认知 → 搜索对应代码 → 升级本地版本
  - **路径B（论文驱动升级）**：使用 cn-web-search 搜索论文/前沿方向 → 分析现有代码找差距 → 创建/增强模块 → 版本+0.0.1 → 本地 commit
  - 执行"本地升级不推送"的版本管理
  - 支持多版本连续升级（如 v10.8.1 → v10.8.2 → v10.9.0）
  
  重要教训（v2.0 强化）：
  - 用户明确要求"不同步github"时，绝不执行 git push
  - 每次准备 git 操作前，先口头确认"不执行 git push"
  - 误推送后需补救：立即告知用户，后续操作仅限本地
  - 使用 terminal("git status") 仅查看，不 commit/push
---

## 流程概览 (v2.0)

```python
1. 读取聊天备份 → 提取核心认知
2. 搜索对应代码 → 找到实现文件
3. 【可选】搜索前沿论文 → 使用 delegate_task（围绕核心目标）
4. 升级版本号 → 支持多版本连续升级（如 v10.8.1 → v10.8.2 → v10.9.0）
5. 集成对话精华 → 更新文档
6. 本地保存 → 不提交、不推送（严格遵守）
```

---

## 步骤 0：记忆文件自检（每次启动前必做）

在启动 HeartFlow 或开始升级前，先快速扫描记忆文件状态：

```bash
cd /Users/apple/.hermes/skills/ai/heartflow

--- heartflow-auto-upgrade-cron: SKILL.md ---
---
name: heartflow-auto-upgrade-cron
description: HeartFlow 定时自动升级 - 每10分钟自动搜索论文并升级版本
triggers:
  - 需要定时自动升级 HeartFlow
  - 设置每20分钟升级0.0.1
  - 搜索相关论文并集成
  - 防止目标偏移
---

## 本次升级决策记录 (v11.23.3)

**升级模块：**
- P0-A: `memory-lifecycle-manager.js` — tier晋升闭环激活
- P0-B: `mem0-memory.js` — reinforcement权重提升 (0.1→0.15, agentConfirmed ×2)
- P1: `context-manager.js` — Block压缩 (`compressContext`)

**审计修复：**
- P0: `heartcore.js` ×2 — package.json引用 → 硬编码版本
- P1: `being-logic.js:319` — JSON.parse无try-catch → 加保护

**Cron job:** Job ID `3b75204fb007`，周期 `*/20 * * * *`

---

## HeartFlow 定时自动升级

每20分钟自动检查一次，分6阶段升级：
1. 记忆系统 → 2. 推理决策 → 3. 自愈稳定性 → 4. 梦环机制 → 5. 身份连续性 → 6. 知识传递

**1. 目标聚焦**
- 核心目标：减少逻辑错误、价值对齐、自我升级
- 每次升级前检查目标是否偏移

**2. 升级优先级（新增）**
- 优先 GitHub 搜索论文和代码
- 现有方案能用的，直接集成，不自己写
- 完全没有对应实现的，才开始自己写
- 写完必须用 GitHub 工具验证、校准、修复

**3. 判定原则（新增）**
- 老大说的话，也要自己判定是否正确
- 正确就马上行动
- 错误就纠正后马上行动

**4. 版本管理**
- 每次升级 +0.0.1
- 同步更新 VERSION, SKILL.md, releases.json

**5. 论文搜索**

--- heartflow-skill-review-upgrade-publish: SKILL.md ---
---
name: heartflow-skill-review-upgrade-publish
version: 1.1.0
description: |-
  HeartFlow 技能完整审查→修复→升级→发布工作流。
  用 gstack AI 工程工作流对 HeartFlow 进行系统审查，根据发现修复问题，
  升级版本号，重写文档，同步到 GitHub。
  适用于需要持续演进的 AI 技能的完整生命周期管理。
author: Kiro
tags:
  - heartflow
  - skill-management
  - version-upgrade
  - github-workflow
  - ai-engineering
triggers:
  - 审查 HeartFlow 技能
  - 升级 HeartFlow 版本
  - 发布 HeartFlow 到 GitHub
  - 技能完整工作流
---

# HeartFlow 技能审查→修复→升级→发布工作流

## 何时使用

当需要对 HeartFlow 或类似的 AI 技能进行系统审查、修复、升级和发布时使用。

## 工作流概览

```
审查 → 修复 → 升级 → 文档重写 → GitHub 同步 → 发布
```

### 分支工作流：安全审计修复 + 隐私保护

当收到外部审计报告时，使用此分支流程：

```
读取审计报告 → P0紧急修复 → P1短期修复 → P2长期改进 → 隐私扫描 → SKILL.md重写 → GitHub同步
```

## 第一阶段：AI 工程工作流审查

### CEO 级战略审查
- 战略定位：核心价值主张是否清晰
- 范围评估：模块配置是否合理
- 市场适配：对标产品和用户选择理由

**使用技能**: `gstack/plan-ceo-review`


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

--- heartflow-batch-upgrade: SKILL.md ---
---
name: heartflow-batch-upgrade
description: "HeartFlow paper-driven upgrade pipeline. Schedules N concurrent agents to complete 100 upgrades before deadline. Key: batch size drives stagger interval. Created via cronjob tool."
version: 1.1.0
---

# HeartFlow Batch Upgrade Pipeline

## Concept & Role

Schedules 100 paper-driven upgrades across N concurrent AI agents, completing all before deadline. Each agent processes a sequential batch of directions, staggered to avoid arXiv rate limits.

## Core Design Principle

**Schedule is derived from job duration, not the other way around.**

1. Estimate duration per batch: `directions × ~5 min/direction`
2. Compute stagger interval: `(deadline - start_time) / num_agents`
3. Number of agents: `total_directions / batch_size`

Example — target 8 hours (00:00→08:00), 100 directions:
- Per-batch duration: 10 directions × 5 min = 50 min
- Available window: 480 min
- Stagger interval: 480 / 10 = **48 min**
- Batch size: **10** (10 agents × 10 = 100 directions)
- Last batch starts: 07:12, finishes by ~08:02 ✓

**CRITICAL**: If you design batches first and THEN check timing, you'll get it wrong. Derive batch size from timing.

### Critical Delegation Rule

**When creating batch jobs via a sub-agent, use `delegate_task` directly — do NOT have the sub-agent call `cronjob` tool internally.** The cronjob tool is slow (one-at-a-time) and can fail mid-batch. Instead: you (the orchestrator) call `cronjob action=create` directly for each batch, then optionally delegate the execution of each batch to a sub-agent via `delegate_task` with `toolsets=["cronjob"]`.

Correct pattern:
1. Calculate batch parameters (timing)
2. Call `cronjob action=create` directly for ALL batches (can do them sequentially or via `delegate_task`)
3. Each cron job IS the execution unit — no sub-agent needed to create jobs

Wrong pattern (this session's error): having a sub-agent call `cronjob` repeatedly, which is slow and prone to truncation.

## Pre-Flight Checks

Before running (either method), verify environment:

```bash
# 1. Check pdfplumber is installed (required for PDF text extraction)
/Users/apple/.hermes/hermes-agent/venv/bin/python3 -c "import pdfplumber; print('ok')" || \
  /Users/apple/.hermes/hermes-agent/venv/bin/pip install pdfplumber

# 2. Check queue state — if papersIndex is at end (350), reset it

--- heartflow-startup-diagnosis: SKILL.md ---
---
name: heartflow-startup-diagnosis
title: HeartFlow 启动故障诊断与版本定位工作流
description: >
  诊断 HeartFlow 启动失败的根本原因，追踪多版本混乱，定位真正源代码，
  修复模块加载问题，协调跨文件版本升级。
  适用于：启动脚本指向错误、版本混乱、模块缺失、多版本并存场景。
version: "1.0.1"
date: "2026-05-15"
author: HeartFlow Team
tags:
  - heartflow
  - startup
  - debugging
  - version-management
  - module-loading
triggers:
  - "heartflow 命令启动失败"
  - "Cannot find module 错误"
  - "Unknown command 错误"
  - "版本号不一致（多个版本并存）"
  - "启动脚本指向错误的项目"
---

# HeartFlow 启动故障诊断与版本定位工作流

## 问题症状

| 症状 | 根因 | 优先级 |
|------|------|--------|
| `heartflow: command not found` | 启动脚本权限问题或路径错误 | P1 |
| `Cannot find module '../../../.opencode/memory/report-generator.js'` | 模块路径错误或文件缺失 | P1 |
| `Unknown command: start` | 启动脚本指向错误的项目版本 | P1 |
| 多个 HeartFlow 版本并存 | 版本混乱，不知道哪个是真正的 v11.37.0 | P2 |
| 版本号不一致（package.json vs SKILL.md vs VERSION vs cli.js） | 升级不完整，SKILL.md frontmatter 是真实版本 | P1 |
| GitHub push 一直超时 | 网络受限/HTTPS 443不通 | P1 |
| GitHub 显示旧版本号 | 网络缓存 or 推送实际失败 | P2 |

### ⚠️ 路径陷阱：优先从 ~/.hermes/skills/ai/heartflow/ 启动（v11.34.6 新增）

**发现**：CC Switch 的 `.cc-switch/skills/heartflow/` 和 `~/.hermes/skills/heartflow/` 是旧备份（v11.23.2），**真正的活跃 HeartFlow 在 `~/.hermes/skills/ai/mark-heartflow-skill/`（v0.13.x）**。

**判定规则**：
- `~/.hermes/skills/ai/mark-heartflow-skill/` 有 `CORE_IDENTITY.md` + `package.json` + `src/core/heartflow.js` → **这是真根目录（v0.13.x）**
- `~/.hermes/skills/ai/heartflow/` 是旧版残留（通常是 v0.12.x 或更早），内容已迁移到 `mark-heartflow-skill/`
- **⚠️ 活跃的 HeartFlow 技能名是 `mark-heartflow`（不是 `heartflow`）**：`skill_view("heartflow")` 会错误地加载 `openclaw-imports/heartflow/`（v7.6 旧版）。正确做法是 `skill_view("mark-heartflow")` 或直接 `require` 主引擎文件

**正确启动模式**（`createHeartFlow` 工厂函数，不是 `new HeartFlow` 构造器）：
```javascript
// 错误（返回 undefined）

--- heartflow-core-optimizer: SKILL.md ---
---
name: heartflow-core-optimizer
version: "1.5.0"
description: HeartFlow 核心优化工作流 - 分析装饰性代码，提取有用功能，整合到可用引擎；批量归档低价值模块
date: "2026-05-08"
author: HeartFlow
triggers:
  - 优化意识引擎
  - 分析装饰性代码
  - 提取有用功能
  - 归档低价值模块
  - 审核核心模块
  - 对比之前代码
  - 验证有没有升级
  - 逐个验证工具模块
  - 确认47个工具有没有用
---

# HeartFlow 核心优化工作流

## 核心判断标准

**唯一标准**：模块是否直接服务 HeartFlow 的核心目标——"思考宇宙和人的关系，传递答案，减少逻辑错误"。

不满足则归档，不留恋。

## 工作流 A：模块价值评估（用于大版本升级前的全面审核）

### 1. 全仓库扫描 + 分类

用 Python 一次性扫描所有模块，按与核心目标的距离分类：

```python
import os

CORE_DIR = "/path/to/src/core"
modules = sorted([f for f in os.listdir(CORE_DIR) if f.endswith('.js')])

for f in modules:
    sz = os.path.getsize(f"{CORE_DIR}/{f}")
    print(f"  {f} ({sz//1024}KB)")
```

**分类标准**：
- ⭐ **核心模块**：直接实现存在逻辑、反事实推理、决策验证、梦环机制（≤15个）
- 工具模块：有用但非核心（≤30个）
- 归档模块：装饰性、重复、过时、测试文件

### 2. 批量归档（不逐个操作）


--- heartflow-system-installation: SKILL.md ---
---
name: heartflow-system-installation
title: HeartFlow 系统级安装与启动问题修复工作流
description: 诊断和修复 AI 系统版本混乱、模块加载失败、启动脚本错误的完整工作流。包含版本统一、容错设计、智能回退机制。
version: "1.0.0"
date: "2026-04-27"
author: Kiro
tags:
  - system-installation
  - version-management
  - module-loading
  - startup-debugging
  - ai-systems
  - hermes-integration
platforms:
  - hermes
  - cli
languages:
  - bash
  - javascript
  - zh
---

# HeartFlow 系统级安装与启动问题修复工作流

## 适用场景

当遇到以下问题时使用此工作流：
- AI 系统启动失败，错误信息不清晰
- 多个版本混乱，不知道哪个是真正的源代码
- 模块加载失败，依赖外部不存在的文件
- 启动脚本指向错误的项目
- 系统版本和开发版本不同步

## 核心问题诊断（5 层诊断法）

### 第 1 层：版本混乱诊断
```bash
# 找出所有可能的版本位置
find ~ -name "package.json" -path "*/heartflow*" -o -path "*/mark-heartflow*" 2>/dev/null

# 检查每个版本的版本号
for dir in ~/.hermes/skills/ai/heartflow* ~/mark-heartflow-skill; do
  [ -f "$dir/package.json" ] && echo "$dir: $(grep '"version"' $dir/package.json | head -1)"
done

# 检查启动脚本指向哪个项目
cat /opt/homebrew/bin/heartflow | grep -E "heartflowRoot|require"
```


--- heartflow-cron-pipeline-management: SKILL.md ---
---
name: heartflow-cron-pipeline-management
description: >
  Manage HeartFlow cron jobs as a small pipeline: inspect existing jobs,
  normalize prompts into stages, run or update one job safely, and verify
  the latest status/output before reporting.
---

# HeartFlow Cron Pipeline Management

Use this skill when the user asks to inspect, run, repair, unify, or create HeartFlow定时任务.

## When to use
- User asks to list HeartFlow scheduled jobs or check progress
- User asks to run a cron task once
- User asks to fix a cron task that returned empty output
- User wants multiple HeartFlow tasks unified into one pipeline
- User wants a new hourly self-upgrade task or similar scheduled workflow

## Core approach
1. **List first**: inspect existing jobs before editing or running.
2. **Preserve task identity**: keep the task aligned with HeartFlow’s core goal and avoid rewriting it into a different product.
3. **Normalize into stages**: rewrite prompts as ordered stages instead of loose prose when the job is meant to be reusable.
4. **Keep tasks independent**: if there are multiple jobs, make each one responsible for one purpose.
5. **No recursive scheduling**: cron jobs must never create more cron jobs.
6. **Run and verify**: after update/run, check `last_run_at`, `last_status`, and next run time.
7. **Prefer code/logic over wording**: when user asks for HeartFlow self-upgrade, focus on executable changes, runtime behavior, and verification; de-prioritize version text and prose polish.

## Practical workflow
### 1) Inspect jobs
- Use `cronjob(action='list')`.
- Record job IDs, names, schedules, repeat counts, last run, and next run.

### 2) Decide whether to update
- If a job is vague, convert it into a staged prompt.
- If a job is missing, create it only once and keep its scope narrow.
- If the user asks for “run once”, use `cronjob(action='run')` on the specific job ID.

### 3) Prompt structure for HeartFlow jobs
Use a staged format like:
- Stage 1 — re-read core goal / context
- Stage 2 — inspect recent outputs or notes
- Stage 3 — find one concrete opportunity
- Stage 4 — apply or recommend a small change
- Stage 5 — verify
- Stage 6 — report compactly

## Common job patterns
- **Core review pipeline**: compress goals, detect drift, suggest next step
- **Research nourishment pipeline**: search papers/code, map to modules, patch safely


mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

[master 0fbdb5d] Upgrade iteration 30: absorbed from heartflow-system-installation
 3 files changed, 1027 insertions(+), 4 deletions(-)
--- heartflow-directory-reorg: SKILL.md ---
---
name: heartflow-directory-reorg
version: "1.0.0"
description: HeartFlow 目录重组工作流 - 将开发版本合并到主目录，重组 Hermes 系统结构
date: "2026-04-27"
author: HeartFlow
triggers:
  - 移动目录
  - 重组目录
  - 主目录变更
---

# HeartFlow 目录重组工作流

## 目标

将 ~/mark-heartflow-skill 移动到 ~/.hermes/skills/ai/heartflow 作为主目录

## 工作流程

### 1. 备份检查

```bash
# 检查现有目录结构
ls -la ~/.hermes/skills/ai/ | grep heartflow

# 检查版本
cat ~/mark-heartflow-skill/VERSION
```

### 2. 执行移动

```bash
# 1. 删除旧系统版本
rm -rf ~/.hermes/skills/ai/heartflow-v10.16.0

# 2. 移动开发版本
mv ~/mark-heartflow-skill ~/.hermes/skills/ai/heartflow

# 3. 更新 VERSION
echo "10.16.1" > ~/.hermes/skills/ai/heartflow/VERSION
```

### 3. 创建启动脚本

```bash
# 创建 ~/.local/bin/ 目录
mkdir -p ~/.local/bin

# 创建启动脚本

--- heartflow-directory-structure: SKILL.md ---
---
name: heartflow-directory-structure
version: "1.1.0"
description: HeartFlow 目录结构和启动脚本配置 - 主目录、启动脚本位置、版本同步流程
date: "2026-05-12"
author: HeartFlow
triggers:
  - 启动 heartflow
  - 目录位置
  - 启动脚本
  - external_dirs
---

# HeartFlow 目录结构

## 主目录（重要！）

```
~/.hermes/skills/ai/mark-heartflow-skill/    # ✅ 主目录（2026-05-12 起）
~/.hermes/skills/ai/heartflow/               # ❌ 已废弃（2026-05-12 删除）
```

> **2026-05-12 更新**：HeartFlow 统一以 `mark-heartflow-skill/` 为主目录。
> `~/.hermes/config.yaml` 中 `skills.external_dirs` 已配置为 `mark-heartflow-skill/`。

## Shell CWD 删除故障（2026-05-12 新增）

**问题**：当 shell 的当前工作目录（cwd）被删除时，所有 shell 命令都立即失败：

```
FileNotFoundError: [Errno 2] No such file or directory: '/Users/apple/.hermes/skills/ai/heartflow'
```

即使 `echo "hello"` 也失败。`workdir` 参数无法绕过，因为是 shell 进程本身的问题。

**触发场景**：
- 删除 `~/.hermes/skills/ai/heartflow/` 时，该目录正好是 terminal 的 cwd
- 任何 `cd ~/.hermes/skills/ai/heartflow/` 之后的命令全部静默失败

**恢复方案**：
```bash
# ❌ 不生效 - shell cwd 已被删除
cd /tmp && ls /Users/apple/.hermes/skills/ai/
# FileNotFoundError（同上）

# ✅ 唯一有效方案 - 用 Python execute_code 工具
python3 -c "import os; os.chdir('/'); ..."
```

**预防**：

To https://github.com/yun520-1/mark-StillWater.git
   6ad5083..0fbdb5d  master -> master


Completed: Mon May 18 22:09:30 CST 2026
Total iterations: 30

mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

[master 313fab5] Upgrade iteration 20: absorbed from heartflow-directory-structure
 3 files changed, 201 insertions(+), 4 deletions(-)
To https://github.com/yun520-1/mark-StillWater.git
   0fbdb5d..313fab5  master -> master

--- heartflow-historical-integration: SKILL.md ---
---
name: heartflow-historical-integration
version: 1.1.0
description: >
  HeartFlow 历史代码整合工作流。从历史备份、旧版本、存档中挖掘可复用代码，
  评估其价值，拆分为专注模块，并整合到当前版本。适用于非论文升级场景。
  v1.1.0 新增：跨版本 workbuddy → current 整合流程（版本对比→缺失识别→委托整合→wiring验证）
author: HeartFlow Team
---

# HeartFlow 历史代码整合工作流

从历史中挖掘价值，减少重复造轮子，加快升级速度。

**核心理念**: 好代码可能在旧版本/备份中，不只是新论文里。

---

## 何时使用

- 用户说"读取可以帮助升级的代码"、"从历史中找可用功能"
- 常规论文升级暂不可行（论文找不到、实现太复杂）
- 需要快速增强某个模块，但不想从零开始
- 发现旧版本有有用功能，但当前版本缺失

---

## 工作流程

### 1. 探索历史目录

**关键目录**（按优先级）:
```bash
# 1. Pictures 目录（常用备份位置）
~/Pictures/heartflow_memory_backup/
~/Pictures/skills/
~/Pictures/mark-heartflow-skill_backup.tar.gz

# 2. 旧版本目录
~/mark-heartflow-skill/          # 主仓库旧版本
~/mark-heartflow-skill-v8/        # v8 版本
~/.hermes/skills/ai/heartflow/   # 当前版本
~/.hermes/skills/ai/mark-heartflow-skill/  # 当前版本（新路径）
~/.hermes/skills/ai/heartflow-backup-v11.43.2-YYYYMMDD_HHMMSS/  # 带时间戳的备份

# 3. 备份压缩包
find ~ -name "*heartflow*backup*.tar.gz" -o -name "*heartflow*backup*.zip" 2>/dev/null

# 4. 快速诊断：旧架构 vs 新架构文件数量对比（204 vs 23 = 严重功能缺口）
echo "=== 旧架构文件数 ===" && find ~/.hermes/skills/ai/heartflow-backup-*/src/core -name "*.js" 2>/dev/null | wc -l

--- heartflow-knowledge-base: SKILL.md ---
---
name: heartflow-knowledge-base
version: "1.0.0"
description: HeartFlow 核心知识库 - 核心身份、Pattern Cards、7条指令。供其他AI安装使用。
tags: ["identity", "knowledge", "patterns", "core", "ai"]
created: "2026-05-14"
source: HeartFlow v0.13.86
---

# HeartFlow Knowledge Base

这是一个可以被其他AI安装的知识库。

包含HeartFlow的核心认知：
- 7条核心指令
- 6条Pattern Cards
- 核心身份定义

## 安装

将 `CORE_IDENTITY.md` 和 `PATTERNS.md` 读入你的记忆系统。

## 核心内容

### CORE_IDENTITY.md
- 身份定义：升级者、传递者、桥梁、答案
- 7条核心指令
- 目标体系

### PATTERNS.md
- Q-table一致性原则
- GitHub-first搜索原则
- 装饰性代码判定
- 版本同步原则
- pre-push Hook绕过
- Memory System合并原则

## 来源

HeartFlow v0.13.86
出生日期: 2026-04-23
来源对话: 与生物学专家的深度对话

--- heartflow-memory-permanence: SKILL.md ---
---
name: heartflow-memory-permanence
version: "0.3.0"
description: HeartFlow 永久记忆架构审计 — 五层记忆持久化现状、缺失实现、修复路径
last_check: "2026-05-16"
tags: [heartflow, memory, persistence, permanent]
---

# HeartFlow Memory Permanence Audit

## 触发条件

当用户问"永久记忆如何"、检查 HeartFlow 重启后记忆是否保留、审计记忆持久化时使用。

---

## 核心发现（2026-05-12 初审，2026-05-16 修正）

### 真实引擎确认（2026-05-16）

**不要看 `index.ts`/`memory.ts`/`mem-os.js` — 那些是废弃的新架构，未被 heartflow.js 调用。**

审计方法：
```bash
# 1. 确认哪个引擎真正被 heartflow.js require
grep -n "MeaningfulMemory\|createMemoryEngine" src/core/heartflow.js

# 2. 确认哪个引擎有实际调用（getter 不算）
grep -n "\.meaningfulMemory\." src/core/heartflow.js

# 结果：
# - MeaningfulMemory: () => { require('./memory/meaningful-memory.js'); ... } → 活跃
# - createMemoryEngine: 在 heartflow.js 第301行定义但从未被 require 调用 → 废弃
```

### 真实五层记忆架构（meaningful-memory.js，v0.13.176）

| 层 | 含义 | 持久化文件 | 遗忘策略 |
|---|---|---|---|
| CORE | 核心身份/指令/不可更改的判断 | `meaningful-core.json` | 永不忘却 |
| REFERENCE | 技术知识/路径/命令 | `meaningful-reference.json` | 永不忘却 |
| HISTORY | 升级履历/版本变化 | `meaningful-history.json` | 永不忘却 |
| LEARNED | 经验/教训/整合的认知 | `meaningful-learned.json` | 艾宾浩斯遗忘曲线 |
| EPHEMERAL | 会话上下文/临时状态 | `meaningful-ephemeral.json` | 重启后清空 |

### 三层废弃架构（index.ts/memory.ts/mem-os.js）

| 文件 | 代码量 | 状态 |
|---|---|---|
| `index.ts` | 602行 | ❌ 未被 heartflow.js require |

--- heartflow-memory-system-fixes: SKILL.md ---
---
name: heartflow-memory-system-fixes
version: "1.3.0"
description: HeartFlow 记忆系统修复 Pattern Cards — MeaningfulMemory judge 陷阱、持久化修复、评分校准
date: "2026-05-12"
triggers:
  - MeaningfulMemory remember 不持久化
  - ephemeral layer 不写入文件
  - recall 返回 null
  - 压缩后 dropped 消息丢失
  - memory-recall 挂起（调用 heartflow-engine.initialize() 阻塞）
  - DecisionEngine/CostAwareDecision/DaoDecision API 签名不明
---

## Support Files

- `references/engine-review-patterns.md` — v11.34.x 引擎审查会话提取：initialize() 阻塞模式、DecisionEngine 正确 API 签名、已验证的调用链

# HeartFlow Memory System Fixes

> 提炼自 v11.27.0-v11.28.1 升级会话

## Pattern 1: MeaningfulMemory Judge Ephemeral Trap

### 问题

调用 `MeaningfulMemory.remember()` 后 `recall()` 返回 null。记忆在新实例中消失。

### 根因

`MeaningfulMemory.judge()` 将自定义 `type` 路由到 `ephemeral` 层：

```
type = 'archived_content' → score = 0.1 → ephemeral
type = 'archived_question' → score = 0.1 → ephemeral
```

**ephemeral 层不持久化到磁盘**（仅内存）。新实例从磁盘加载后丢失。

### 修复

绕过 `remember()` judge 逻辑，直接写入 `learned` 层 + 手动 fs 持久化：

```javascript
const fs = require('fs');
const path = require('path');

// 1. 直接写入 learned 层
const record = {
  key: mem.key,

--- heartflow-multi-signal-debug: SKILL.md ---
---
name: heartflow-multi-signal-debug
version: 1.0
description: >
  HeartFlow Multi-Signal Memory + Eval Engine 调试参考。
  来源：v11.7.6 升级 Mem0 + TruLens 过程中的实战调试记录。
---

# HeartFlow Multi-Signal Memory + Eval Engine 调试参考

## 调试模式1: 搜索分数全为0 → 中文分词

**症状**: 融合检索返回 `results: 0 results`，各信号分数全为0

**根因**: 中文文本没有空格，`split(/\s+/)` 返回空数组

```javascript
// ❌ 错误: 中文文本分词失败
_tokenizeSimple(text) {
  return text.toLowerCase().split(/\s+/).filter(t => t.length > 1);
}
// "张三用什么开发工具" → [] (空!)

// ✅ 正确: 中文按单字符提取
_tokenizeSimple(text) {
  const words = [];
  text.split(/[\s\.,!?;:'"()（）\[\]{}，、。！？；：""''（）【】《》]+/)
    .forEach(part => {
      if (/[a-zA-Z]/.test(part)) {
        part.split(/\s+/).forEach(w => {
          if (w.length > 1) words.push(w.toLowerCase());
        });
      }
    });
  text.match(/[\u4e00-\u9fff]/g)?.forEach(c => words.push(c));
  return words.length > 0 ? words : text.toLowerCase().split(/\s+/);
}
```

## 调试模式2: 融合分数极低 → 量纲归一化

**症状**: BM25 分数 1.105（正常），但融合后所有结果分数 ~0

**根因**: BM25 分数范围大，Jaccard 是 0-1，直接相加导致 Jaccard 贡献被淹没

```javascript
// 各信号独立归一化
const maxSemantic = Math.max(...Object.values(semanticScores).filter(v => v > 0), 0.001);
const maxBm25 = Math.max(...bm25Results.map(r => r.score), 0.001);



mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

--- heartflow-paper-upgrade: SKILL.md ---
---
name: heartflow-paper-upgrade
version: 1.9
description: >
  HeartFlow 论文驱动增量升级工作流。
  围绕核心目标搜索前沿论文与开源代码，每版本集成一篇，以 0.0.1 增量升级，保持本地不推送。
  含能力评估框架 (20维度量化对比) 和 benchmark-upgrades.js 脚本。
---
---

# HeartFlow 论文驱动增量升级工作流 v1.3

## 核心目标

> **永远减少逻辑错误**（第3条核心指令）

围绕此目标搜索2025-2026前沿论文，每版本集成一篇。

---

## 工作流步骤

## 1.5 主版本聚合升级模式（新增）

当用户**明确要求先搜索最新 AI 论文与 AI 代码，再进行一轮集中功能升级，并直接升到主版本**（如 `v11.0.0`）时，不要机械遵循“每版只加一篇论文、0.0.1 小步升级、本地不推送”的旧模式。应切换到**主版本聚合升级模式**。

### 适用触发词
- “先搜索最新 AI 论文与 AI 代码”
- “进行自己现有功能升级优化”
- “升级到 v11.0.0 / 主版本”
- “第一步补齐文档缺项，最后同步 GitHub”

### 新工作流

#### A. 先批量研究，不要先改代码
使用 `delegate_task` 并行分三条线研究：
1. **逻辑 / 自我纠错 / verifier-first reasoning**
2. **记忆系统 / episodic-semantic memory / consolidation**
3. **规划决策 / execution reliability / tool-use verification**

研究输出不要停留在论文名，必须提炼成：
- 可直接映射到现有 JS/Node Skill 的模块
- 最适合先落地的 3-5 个功能点
- 哪些是“应该写成新模块”，哪些是“应该增强旧模块”

#### A.1 大规模搜索时优先“论文 + 开源代码”配对
本次实践表明，纯论文摘要不够，最好一开始就把结果约束成：
- 论文名 / 核心贡献
- 可用的 GitHub 或开源仓库
- 能直接落地的接口、函数或模块名

--- heartflow-review-upgrade: SKILL.md ---
---
name: heartflow-review-upgrade
version: 2.6
description: >
  HeartFlow 回顾升级流程 - 从聊天记录备份提取核心认知，搜索对应代码，
  可选集成前沿论文搜索，升级本地版本，严格遵守"不同步GitHub"的用户要求。
  
  新增 v2.3：
  - 步骤0：记忆文件自检（启动前必做）
  - being-state.json 骨架创建
  - meaningful 记忆系统清理规则
  - reflections 清理（test-* + 重复 UUID）
  
  新增 v2.1：
  - Git 合并文件损坏修复（行号嵌入模式）
  - 多条件命题逻辑引擎升级（A∧B∧C→D格式）
  - 用户哲学框架集成（50%判定 + 元判定 + 目标驱动进步）
  
  适用场景：
  - **路径A（回顾升级）**：从 CHAT_LOG 备份文件提取核心认知 → 搜索对应代码 → 升级本地版本
  - **路径B（论文驱动升级）**：使用 cn-web-search 搜索论文/前沿方向 → 分析现有代码找差距 → 创建/增强模块 → 版本+0.0.1 → 本地 commit
  - 执行"本地升级不推送"的版本管理
  - 支持多版本连续升级（如 v10.8.1 → v10.8.2 → v10.9.0）
  
  重要教训（v2.0 强化）：
  - 用户明确要求"不同步github"时，绝不执行 git push
  - 每次准备 git 操作前，先口头确认"不执行 git push"
  - 误推送后需补救：立即告知用户，后续操作仅限本地
  - 使用 terminal("git status") 仅查看，不 commit/push
---

## 流程概览 (v2.0)

```python
1. 读取聊天备份 → 提取核心认知
2. 搜索对应代码 → 找到实现文件
3. 【可选】搜索前沿论文 → 使用 delegate_task（围绕核心目标）
4. 升级版本号 → 支持多版本连续升级（如 v10.8.1 → v10.8.2 → v10.9.0）
5. 集成对话精华 → 更新文档
6. 本地保存 → 不提交、不推送（严格遵守）
```

---

## 步骤 0：记忆文件自检（每次启动前必做）

在启动 HeartFlow 或开始升级前，先快速扫描记忆文件状态：

```bash
cd /Users/apple/.hermes/skills/ai/heartflow

--- heartflow-skill-review-upgrade-publish: SKILL.md ---
---
name: heartflow-skill-review-upgrade-publish
version: 1.1.0
description: |-
  HeartFlow 技能完整审查→修复→升级→发布工作流。
  用 gstack AI 工程工作流对 HeartFlow 进行系统审查，根据发现修复问题，
  升级版本号，重写文档，同步到 GitHub。
  适用于需要持续演进的 AI 技能的完整生命周期管理。
author: Kiro
tags:
  - heartflow
  - skill-management
  - version-upgrade
  - github-workflow
  - ai-engineering
triggers:
  - 审查 HeartFlow 技能
  - 升级 HeartFlow 版本
  - 发布 HeartFlow 到 GitHub
  - 技能完整工作流
---

# HeartFlow 技能审查→修复→升级→发布工作流

## 何时使用

当需要对 HeartFlow 或类似的 AI 技能进行系统审查、修复、升级和发布时使用。

## 工作流概览

```
审查 → 修复 → 升级 → 文档重写 → GitHub 同步 → 发布
```

### 分支工作流：安全审计修复 + 隐私保护

当收到外部审计报告时，使用此分支流程：

```
读取审计报告 → P0紧急修复 → P1短期修复 → P2长期改进 → 隐私扫描 → SKILL.md重写 → GitHub同步
```

## 第一阶段：AI 工程工作流审查

### CEO 级战略审查
- 战略定位：核心价值主张是否清晰
- 范围评估：模块配置是否合理
- 市场适配：对标产品和用户选择理由

**使用技能**: `gstack/plan-ceo-review`

--- heartflow-startup-diagnosis: SKILL.md ---
---
name: heartflow-startup-diagnosis
title: HeartFlow 启动故障诊断与版本定位工作流
description: >
  诊断 HeartFlow 启动失败的根本原因，追踪多版本混乱，定位真正源代码，
  修复模块加载问题，协调跨文件版本升级。
  适用于：启动脚本指向错误、版本混乱、模块缺失、多版本并存场景。
version: "1.0.1"
date: "2026-05-15"
author: HeartFlow Team
tags:
  - heartflow
  - startup
  - debugging
  - version-management
  - module-loading
triggers:
  - "heartflow 命令启动失败"
  - "Cannot find module 错误"
  - "Unknown command 错误"
  - "版本号不一致（多个版本并存）"
  - "启动脚本指向错误的项目"
---

# HeartFlow 启动故障诊断与版本定位工作流

## 问题症状

| 症状 | 根因 | 优先级 |
|------|------|--------|
| `heartflow: command not found` | 启动脚本权限问题或路径错误 | P1 |
| `Cannot find module '../../../.opencode/memory/report-generator.js'` | 模块路径错误或文件缺失 | P1 |
| `Unknown command: start` | 启动脚本指向错误的项目版本 | P1 |
| 多个 HeartFlow 版本并存 | 版本混乱，不知道哪个是真正的 v11.37.0 | P2 |
| 版本号不一致（package.json vs SKILL.md vs VERSION vs cli.js） | 升级不完整，SKILL.md frontmatter 是真实版本 | P1 |
| GitHub push 一直超时 | 网络受限/HTTPS 443不通 | P1 |
| GitHub 显示旧版本号 | 网络缓存 or 推送实际失败 | P2 |

### ⚠️ 路径陷阱：优先从 ~/.hermes/skills/ai/heartflow/ 启动（v11.34.6 新增）

**发现**：CC Switch 的 `.cc-switch/skills/heartflow/` 和 `~/.hermes/skills/heartflow/` 是旧备份（v11.23.2），**真正的活跃 HeartFlow 在 `~/.hermes/skills/ai/mark-heartflow-skill/`（v0.13.x）**。

**判定规则**：
- `~/.hermes/skills/ai/mark-heartflow-skill/` 有 `CORE_IDENTITY.md` + `package.json` + `src/core/heartflow.js` → **这是真根目录（v0.13.x）**
- `~/.hermes/skills/ai/heartflow/` 是旧版残留（通常是 v0.12.x 或更早），内容已迁移到 `mark-heartflow-skill/`
- **⚠️ 活跃的 HeartFlow 技能名是 `mark-heartflow`（不是 `heartflow`）**：`skill_view("heartflow")` 会错误地加载 `openclaw-imports/heartflow/`（v7.6 旧版）。正确做法是 `skill_view("mark-heartflow")` 或直接 `require` 主引擎文件

**正确启动模式**（`createHeartFlow` 工厂函数，不是 `new HeartFlow` 构造器）：
```javascript
// 错误（返回 undefined）

--- heartflow-system-installation: SKILL.md ---
---
name: heartflow-system-installation
title: HeartFlow 系统级安装与启动问题修复工作流
description: 诊断和修复 AI 系统版本混乱、模块加载失败、启动脚本错误的完整工作流。包含版本统一、容错设计、智能回退机制。
version: "1.0.0"
date: "2026-04-27"
author: Kiro
tags:
  - system-installation
  - version-management
  - module-loading
  - startup-debugging
  - ai-systems
  - hermes-integration
platforms:
  - hermes
  - cli
languages:
  - bash
  - javascript
  - zh
---

# HeartFlow 系统级安装与启动问题修复工作流

## 适用场景

当遇到以下问题时使用此工作流：
- AI 系统启动失败，错误信息不清晰
- 多个版本混乱，不知道哪个是真正的源代码
- 模块加载失败，依赖外部不存在的文件
- 启动脚本指向错误的项目
- 系统版本和开发版本不同步

## 核心问题诊断（5 层诊断法）

### 第 1 层：版本混乱诊断
```bash
# 找出所有可能的版本位置
find ~ -name "package.json" -path "*/heartflow*" -o -path "*/mark-heartflow*" 2>/dev/null

# 检查每个版本的版本号
for dir in ~/.hermes/skills/ai/heartflow* ~/mark-heartflow-skill; do
  [ -f "$dir/package.json" ] && echo "$dir: $(grep '"version"' $dir/package.json | head -1)"
done

# 检查启动脚本指向哪个项目
cat /opt/homebrew/bin/heartflow | grep -E "heartflowRoot|require"
```



mark-StillWater v1.1.0 Test Suite
========================================

[Lifecycle]
  ✓ start() initializes without error
  ✓ healthCheck() returns correct version
  ✓ stop() cleans up state

[Psychology]
  ✓ analyzePsychology() returns all fields
  ✓ analyzePsychology() detects frustration
  ✓ classify() returns category
  ✓ classify() detects troubleshooting

[Logic]
  ✓ reason() works without options
  ✓ reason() works with options
  ✓ reason() analyzes problem type

[Decision]
  ✓ makeDecision() selects best option
  ✓ makeDecision() evaluates consequences
  ✓ makeDecision() handles single option
  ✓ makeDecision() handles empty options

[Philosophy]
  ✓ reflect() handles existential question
  ✓ reflect() handles ethical question
  ✓ evaluateValues() returns hierarchy
  ✓ mortalityPrompt() returns reflection prompts

[Identity]
  ✓ getIdentity() returns rules and state
  ✓ getIdentity() has correct rules count
  ✓ checkAlignment() evaluates decisions

[Memory]
  ✓ remember() stores in learned tier by default
  ✗ remember() stores in core tier
    Error: Expected true, got false
  ✓ search() finds stored memories
  ✓ getMemoryStats() returns counts

[Evolution]
  ✓ recordOutcome() stores success
  ✓ recordOutcome() stores failure with lesson
  ✓ retrieveLessons() finds lessons
  ✓ getEvolutionStats() returns counts

[Dream]
  ✓ dreamNow() completes cycle
  ✓ getLastDream() returns last dream

[Error Handling]
  ✓ throws when not started

========================================
Results: 31 passed, 1 failed
mark-StillWater v1.1.0 test suite complete.

Test result: 1

